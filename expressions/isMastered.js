/*Write the function isMastered which accepts two scores and returns
a Boolean indicating whether either score is greater than or equal to the standard score of 70.*/

    //... declare your function.
function isMastered(first, second)
    {
    return first >= 70 || second >= 70;
    }